# =========================
# Example environment file
# Copy to ".env" and edit values
# Do NOT commit your real ".env"
# =========================

# ---- Postgres container init (used by the db service) ----
POSTGRES_DB=systems_monitor
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME

# ---- App / sampler DB connection ----
# For Docker Compose (recommended):
DB_HOST=db
DB_PORT=5432
DB_NAME=systems_monitor
DB_USER=postgres
DB_PASSWORD=CHANGE_ME

# If you run the app on your HOST (not inside Docker),
# and you mapped host port 5433 -> container 5432, use:
# DB_HOST=localhost
# DB_PORT=5433

# ---- Sampler tuning ----
# Your code uses seconds (based on SAMPLE_INTERVAL=5).
SAMPLE_INTERVAL=5
BATCH_SIZE=1
# You can adjust these values for more or less frequent sampling.