# Future Work

- Add server-side aggregation (avg/min/max per minute bucket)
- Replace polling with WebSockets / SSE
- Add per-core CPU chart
- Add alert thresholds (CPU > 80%, disk > 90%)
- Add data retention (delete rows older than N days)
- Add export button (CSV)
- Add in overhead tests
